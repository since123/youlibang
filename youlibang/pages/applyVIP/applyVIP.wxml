<!--pages/applyVIP/applyVIP.wxml-->
<view class='firstDiv'>
  <view class='minbox'>
    <view class='rechargeImg'><image src='../../images/recharge@2x.png'></image></view>
    <view class='recharge'>充值</view>
    <view class='righttext' bindtap="returnPage">{{buttonText}}</view>
  </view>
  <view class='line'></view>

  <!-- <view class="header_lun" >
    <swiper class="swiper" indicator-dots="true" indecator-color="rgba(120,120,120,1)" autoplay="true" bindchange="">
      <block wx:for="{{movies}}" wx:key="{{movies}}" wx:for-index="index">
        <swiper-item>
          <image src="{{item.url}}" class="slide-image" bindtap="changePath" data-id="{{item.id}}"/>
        </swiper-item>
      </block>
    </swiper>
  </view> -->

  <view class='vipGrade'>
    <swiper indicator-dots="true" indecator-color="rgba(120,120,120,1)" autoplay="true" bindchange="">
      <block wx:for="{{vipRules}}" wx:key="{{item.index}}" >
      <swiper-item>
      <view wx:for="{{vipRules[index]}}" wx:key="{{item.index}}">
        <view bindtap="chooseGrade" data-current="{{index}}" data-rechargemoney="{{item.rechargeMoney}}">
          <view class='mindiv' style="{{index == currentIndex?'border-color:red':'border-color:gray'}}">
              <view class='viptext'>{{item.userGrade}}</view>
              <view class='money'><label>￥</label>{{item.rechargeMoney}}</view>
              <view class='vipnum'>{{item.preferentialInfo}}</view>
          </view>
        </view>
      </view>
      </swiper-item>
      </block>
    </swiper>
    <view class="informlist" hidden="{{informHidden}}">
      <text>优惠明细</text>
        <view  wx:for="{{vipRules}}" wx:key="{{item.index}}" >
          <view  class="inform" wx:for="{{vipRules[index]}}" wx:key="{{item.index}}">
          <text>{{item.userGrade}}充值</text>：{{item.rechargeMoney}}，优惠活动：{{item.preferentialInfo}}
          </view>
        </view>
      </view>
  </view>
      
      <!-- <view class='mindiv' bindtap="chooseGrade" data-id="{{index}}">
          <view class='viptext'>入门VIP</view>
          <view class='money'><label>￥</label>{{introductionRecharge}}</view>
          <view class='vipnum'>限额{{limitsNumber}}名</view>
      </view>
      <view class='mindiv' bindtap="chooseGrade">
          <view class='viptext'>初级VIP</view>
          <view class='money'><label>￥</label>{{juniorRecharge}}</view>
          <view class='vipnum'>充值可得免费铺货{{juniorMoney}}元</view>
      </view>
      <view class='mindiv'>
          <view class='viptext'>中级VIP</view>
          <view class='money'><label>￥</label>{{middleRecharge}}</view>
          <view class='vipnum'>充值可得免费铺货{{middleMoney}}元</view>
      </view> -->
  
  <view class='tuijian'>推荐ID</view>
  <view class='inputInfo'><input type='text' bindinput="getInputValue" placeholder='请输入'>{{inputID}}</input></view>
  <view hidden='{{ifhidden}}'>
    <view class='uploadIDCard'>上传身份证</view>
    <view class='IDCard'>
        <view class='idcardImg'><image src='{{frontID}}' bindtap="changeImage" bindtap="changeFrontID"></image></view>
        <view class='idcardImg'><image src='{{backID}}' bindtap="changeImage" bindtap="changeBackID"></image></view>
    </view>
    <view class='uploadIDCard'>上传营业执照</view>
    <view class='addImg'>
        <view class='imginfo'><image src="{{businessLicense}}" bindtap="changeBusinessLicense"></image></view>
    </view>
  </view>
  <view class='apply_now' bindtap="applyClick">立即申请</view>
</view>
